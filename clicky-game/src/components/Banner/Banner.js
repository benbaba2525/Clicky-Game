import React from 'react';
import './Banner.css';

const Banner = () => (
    <div className="banner text-center d-flex align-items-center justify-content-center">    
        <p className = "flicker-1 header-font">Click on an image to earn points, But don't click on any more than once!!</p>
    </div>
);

export default Banner;